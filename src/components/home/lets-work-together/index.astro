---

// IMPORT ASTRO ATOMS
import Link from "@/atoms/links/index.astro"

// HARDCODED DATA
const data = {

    link: "/",
    title: "Let's Work Together",

}

---

<Link href={data.link}>
    <section class="bg-white hover:bg-light_green duration-300 py-28">
        <div class="space-y-10">
            <div class="flex gap-16 md:gap-24 overflow-hidden">
                <div class="flex gap-16 md:gap-24 items-center whitespace-nowrap animate-slide-left">
                    <p class="text-7xl md:text-[160px] font-semibold font-good_sans">{ data.title }</p>
                    <p class="text-7xl md:text-[160px] font-semibold font-good_sans">{ data.title }</p>
                </div>
            </div>
            <div class="flex gap-16 md:gap-24 overflow-hidden">
                <div class="flex gap-16 md:gap-24 items-center whitespace-nowrap animate-slide-right">
                    <p class="text-7xl md:text-[160px] font-semibold font-good_sans">{ data.title }</p>
                    <p class="text-7xl md:text-[160px] font-semibold font-good_sans">{ data.title }</p>
                </div>
            </div>
        </div>
    </section>
</Link>
